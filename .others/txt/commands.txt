python -m ensurepip --default-pip
python -m pip install --upgrade pip
pip install virtualenv
virtualenv env

activate
pip install Flask
pip install Flask-SQLAlchemy
pip install psycopg2-binary

python -m pip install Jinja2

---->BANCO
service postgresql status
psql -h localhost -U postgres -d green_line
Get-Service postgresql
Get-Service -Name "postgresql-x64-16"



====================[PIPENV]=======================
pip install pipenv
pipenv --python 3.12
pipenv shell
pipenv install Flask
pipenv install SQLAlchemy
pipenv install Flask-SQLAlchemy
pipenv install psycopg2-binary

pip install psycopg2-binary

pipenv run python app.py

setx PATH "%PATH%;D:\00-PROGRAMS\PostgreSQL\bin"
setx PATH "%PATH%;C:\Program Files\PostgreSQL\16\bin"

-> Remover ambient virtual
envpipenv --rm

pipenv install --ignore-pipfile psycopg2-binary
python -c "import psycopg2"

python3 -m pip install psycopg2-binary

===============================================================
# Crie a URL de conexão com o PostgreSQL
db_url = f'postgresql://{db_config["user"]}:{db_config["password"]}@{db_config["host"]}:{db_config["port"]}/{db_config["dbname"]}'

# Configure o SQLAlchemy
app.config['SQLALCHEMY_DATABASE_URI'] = db_url
db=SQLAlchemy(app)