-> NO POWERSHELL DO WINDOWS: Set-ExecutionPolicy Unrestricted

-------------> INSTALANDO AMBIENTE VIRTUAL
python -m ensurepip
python -m pip install virtualenv
pip3 install virtualenv
virtualenv env

pip3 install flask flask-sqlalchemy
pip3 install pg8000
pip3 install psycopg2
pip3 install psycopg2-binary

-------------> INSTALANDO BANCO DE DADOS
activate
python
from app import app, db

# Cria um contexto de aplicação Flask
app.app_context().push()

# Agora você pode criar as tabelas
db.create_all()

-------------> INSTALANDO PSYCOPG2
pip install pipenv
python.exe -m pip install --upgrade pip

pipenv shell
pipenv install psycopg2 flask flask

------->LEMBRE-SE de colocar o path no windows da pasta bin do postgresql: D:\00-PROGRAMS\PostgreSQL\bin

activate
pip install psycopg2-binary
pip install --no-binary :all: psycopg2


pip install pipdeptree
pipdeptree

pip install Flask==2.2.5
pip uninstall psycopg2